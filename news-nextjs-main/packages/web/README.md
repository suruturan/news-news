# 🌐 News Platform — Web (Next.js)

Это web-версия новостного приложения, созданная на **Next.js**.  
Задача проекта — получать новости, отображать список и детали новости.

Этот документ объясняет, как запустить приложение даже если вы никогда не работали с Next.js.


# ⚙️ 1. Установка зависимостей

📌 Перейдите в папку web:

cd packages/web

📌 Установите зависимости:
pnpm install

Если pnpm не установлен:
npm install -g pnpm

# 🔑 2. Настройка переменных окружения
Некоторые функции (например, запросы к NewsAPI) требуют API-ключа.
Даже если ключа нет — приложение всё равно запустится, потому что есть mock-фоллбек.

Создайте файл:
packages/web/.env
Добавьте переменную:
NEWS_API_KEY=your_api_key_here

Если ключ отсутствует — используйте любое значение, например:
NEWS_API_KEY=dev
# ▶ 3. Запуск приложения
Выполните:
pnpm dev

После запуска появится:
Local: http://localhost:3000
Откройте эту ссылку в браузере — web-приложение готово.

# 🧪 4. Проверка работоспособности
- Главная страница загружается
- Новости отображаются
- При отсутствии API-ключа используются mock-данные
- Переходы работают

# 🗂 Структура проекта
Проект использует Next.js App Router.
Структура упрощена под FSD, модули разделены по фичам.

web/
├── app/                  # Страницы и роуты Next.js
│   ├── api/              # API-роуты (прокси к NewsAPI + mock fallback)
│   └── page.tsx          # Главная страница
├── shared/               # Общие UI-компоненты и утилиты
├── features/             # Логика отдельных фич (например, Card, NewsList)
├── package.json
└── README.md

# ❗ Возможные ошибки и решения
"Module not found"
Обычно появляется, если зависимостей нет:
pnpm install

"NEWS_API_KEY missing"
Создайте .env файл.

Страница пустая?
Перезапустите проект:
pnpm dev

🎉 Готово!
Web-версия полностью готова к разработке и тестированию.
